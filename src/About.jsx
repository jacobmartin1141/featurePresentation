import React, {
    useState,
    useEffect,
    useRef,
} from 'react';

function About() {
    return(<section class="page">
        <a href="/app">Go to app</a>
    </section>)
}

export default About;